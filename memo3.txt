- CSV の日付フォーマットは YYYY/MM/DD か YYYY-MM-DD か、両方対応か？
YYYY/MM/DDにのみ対応でよい

- メンバー名のマスタ管理は必要か？（未登録メンバーの検出、表記ゆれ対応など）
不要

- JSON の年月キー・日キーの厳密なフォーマット（先頭ゼロの有無など）は？
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Monthly Workday Schema",
  "type": "object",
  "patternProperties": {
    "^[0-9]{4}-(0[1-9]|1[0-2])$": {
      "type": "object",
      "patternProperties": {
        "^(0[1-9]|[12][0-9]|3[01])$": {
          "type": "integer",
          "enum": [0, 1],
          "description": "0 = Saturday/Sunday, 1 = Weekday"
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
```

- 消化遅延・余剰の判定基準（しきい値）は？（例: 理想線から ±10% など）
±5%

- プロジェクト一覧画面のソート順、フィルタリング機能は必要か？
不要

- チャートライブラリの選定は？（matplotlib, plotly, etc.）
plotly

- 実績線の補間方法は？（直線補間、ステップ補間など）
直線補間

- メンバー別稼働状況の表示粒度は？（日次、週次、月次）
月次

- メンバー別稼働状況の可視化形式は？（棒グラフ、積み上げ棒グラフ、カレンダー、ヒートマップなど）
積み上げ棒グラフ

- 工数余剰の検出基準（しきい値）は？
正確な数値で検出

- 要因候補の推定ロジックの詳細（優先順位、信頼度など）は？
信頼度

- 再読み込み時のデータ整合性チェック（削除されたプロジェクトに紐づく工数実績の扱いなど）は？
プロジェクトの削除はないが、予定工数は変更する可能性はある
メンバーも入力データが変わることがある。
データの整合性はCSVデータで上書きできるようにして欲しい

- レポートのエクスポート形式は？（PDF, Excel, CSV など）
CSVまたはJSON

- レポートの表示項目、レイアウトは？
各プロジェクトの残工数(利益)、個人の負荷率

- 想定ファイルサイズ（プロジェクト数、工数実績行数、休日マスタ年数）は？
プロジェクト数: 50
工数実績行数: 5000
休日マスタ年数: 10

- パフォーマンス測定環境（PC スペック）は？
業務用ノートPC

- UI デザインのモックアップは？
無し

- グラフのインタラクティブ機能の詳細は？（ズーム、ホバー時の数値表示など）
無し

- 単体テストのカバレッジ目標値は？（80% 以上が一般的だが、プロジェクトによる）
80%

- リンター / フォーマッタの選定は？（black, flake8, mypy など）
quality-fixコマンドに準拠する

- ログファイルの保存場所は？（ユーザーホームディレクトリ、アプリケーションディレクトリなど）
アプリケーションディレクトリ

- ログローテーション（ファイルサイズ、世代管理）は必要か？
不要

- データバックアップのエクスポート形式は？（CSV, JSON, SQLite など）
CSV, JSON

- リストア時のデータ整合性チェックは必要か？
不要

- データストアの暗号化は必要か？（個人情報保護の観点）
不要

- 想定稼働時間は？（1 日 8 時間、週 5 日など）
1 日 30分

- メモリリーク検出ツールの選定は？
quality-fixコマンドに準拠する

- Python バージョンは？（3.10, 3.11, 3.12 など）
3.12

- 使用可能なライブラリは？（pandas, matplotlib, plotly, PySide6, Tkinter など）
全て使用可能

- インストール方法は？（実行ファイル、pip install、uv run など）
uvコマンド

- デスクトップアプリの GUI フレームワークは？（PySide6, Tkinter, Kivy など）
PySide6

- データストアの形式は？（SQLite, JSON, CSV など）
SQLite